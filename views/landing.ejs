<!-- Null acct_in or acct_out for a transaction should be displayed as "outside source" -->
<!-- Don't allow "outside source" to show up as an option for either if the other selected source is "outside source" -->
<!-- negative balances are fine since these are budget accounts not bank accounts -->
<!-- The database doesn't calculate account totals -- these must be calculated as part of the route for each transfer. -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Budget Dashboard</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>

<body>
    <%- include('partials/header') %>

    <!-- Welcome Message -->
    <section class="welcome-section">
        <h2>Hi, <%= user.first_name %></h2>
    </section>

    <!-- Error Message -->
    <% if (typeof error_message !== 'undefined' && error_message) { %>
        <div class="alert alert-danger text-center"><%= error_message %></div>
    <% } %>

    <!-- Overview Section -->
    <section class="overview">
        <div class="overview-card income">
            <h3>Monthly Income</h3>
            <p>$<%= monthlyIncome %></p>
        </div>
        <div class="overview-card expenses">
            <h3>Monthly Expenses</h3>
            <p>$<%= monthlyExpenses %></p>
        </div>
        <div class="overview-card net">
            <h3>Net for the Month</h3>
            <p>$<%= net %></p>
        </div>
    </section>

    <!-- Search Bar -->
    <form action="/" method="GET" style="
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: 12px;
    margin: 30px auto;
    width: 90%;
    max-width: 900px;
    ">
    <input 
        type="text" 
        name="search" 
        placeholder="Search by description or account name"
        value="<%= search %>"
        style="
            flex: 1;
            padding: 14px 16px;
            border: 1px solid #d1d1d1;
            border-radius: 8px;
            font-size: 16px;
        "
    />

    <button type="submit" class="search-btn">Search</button>

    <a href="/" class="clear-btn">Clear</a>
    </form>    

    <!-- Transactions Section -->
    <section class="transactions">
        <div class="transactions-header">
            <h3>Recent Transactions</h3>
            <a href="/addEditTransaction" class="add-btn">+ Add Record</a>
        </div>

        <div class="transactions-table-container">
            <table class="transactions-table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Amount</th>
                        <th>Description</th>
                        <th>Account</th>
                        <th>Date</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <% if (transactions && transactions.length > 0) { %>
                        <% transactions.forEach((transaction, index) => { %>
                            <tr>
                                <td><%= index + 1 %></td>
                                <td class="<%= transaction.to_account ? 'positive' : 'negative' %>">
                                    <%= transaction.to_account ? '+' : '-' %>$<%= transaction.amount %>
                                </td>
                                <td><%= transaction.description || 'N/A' %></td>
                                <td>
                                    <%= transaction.from_account || transaction.to_account || 'N/A' %>
                                </td>
                                <td><%= new Date(transaction.date).toLocaleDateString() %></td>
                                <td>
                                    <a href="/addEditTransaction?transactionid=<%= transaction.transactionid %>" class="edit-btn">Edit</a>
                                    <form method="POST" action="/deleteTransaction/<%= transaction.transactionid %>" style="display: inline;">
                                        <button type="submit" class="delete-btn"
                                                onclick="return confirm('Are you sure you want to delete this transaction?')">
                                            Delete
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="6" class="text-center text-muted">No transactions found.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </section>
</body>
</html>
